// Mock for @/components/ui/LoadingSpinner
// Mapped via moduleNameMapper so tests can find the spinner by testid.
export const LoadingSpinner = () => (
  <div data-testid="loading-spinner">Loading...</div>
);
