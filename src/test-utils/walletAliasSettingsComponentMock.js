// Mock for @/components/settings/WalletAliasSettings (component, not hook)
// Used in GiveDashboard tests as a stub for the settings sub-panel.
export const WalletAliasSettings = () => (
  <div data-testid="wallet-settings">Wallet Alias Settings</div>
);
